# GDM222 (2/2565) Portal
A portal for GDM 222 (2/2565) class, KMUTT.

## Project building
Go to the top level directory.
Use `flutter clean`.
Use `flutter create .`.
Use `flutter build web --release` to build the project that targets web.